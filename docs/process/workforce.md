# Организация труда ggrs: архитектор и исполнители

Роль Claude (Fable) — **архитектор и главный инженер**: декомпозиция фаз, брифы с
точными интерфейсами, жёсткое ревью каждого дифа, вердикты, интеграция, критические
фиксы. Рутинный код пишут исполнители. Правило эскалации: две проваленных попытки
исполнителя → уровень выше; три → задача возвращается архитектору как ошибка брифа.

## Матрица исполнителей

| Исполнитель | Канал | Сильные стороны | Слабости (наблюдённые) | Что давать |
|---|---|---|---|---|
| **deepseek-v4-flash** (лайт) | `dev_agent.py` (tool calls: файлы репо + cargo через раннер); режим `:thinking` включаемый | Быстрый, копеечный, дисциплинированно следует системному промпту, честно отчитывается | Не завершает работу без принуждения (нужен финальный форс), слабая инициатива | Транскрипция кода из полного брифа, мелкие правки, тесты по шаблону, механика; `:thinking` — когда чуть сложнее, но про не нужен |
| **deepseek-v4-pro** (thinking) | `dev_agent.py` / `research_agent.py` с `reasoning_effort=high` | Сильное рассуждение, лучший синтез из всех лайтов, надёжный tool-цикл | Дороже и медленнее лайта; reasoning_content обязан пересылаться назад (иначе 400) | Алгоритмическая новизна (backward-математика, OutProd), отладка после провала лайта, исследовательский синтез |
| **Haiku** (субагент) | Agent tool | Полный доступ к инструментам харнесса (Read/Edit/Bash/git), аккуратная транскрипция | Жжёт квоту сессии Anthropic; на нетривиальном плавает | Многофайловые правки по детальному брифу, git-операции, когда нужен родной тулинг |
| **Sonnet 5** (субагент) | Agent tool | Суждение, интеграция, качественное ревью | Квота; дорог для рутины | Интеграционные задачи, ревью больших дифов в помощь архитектору, тупики DeepSeek |
| kr/qwen3-coder-next и др. (OmniRoute) | `omni.py` (без tool calls, файловые блоки) | Бесплатно; qwen — лучший из бесплатных | Нет инструментов (код вслепую), форматные глюки, minimax галлюцинирует tools | Резерв, когда DeepSeek-баланс/квоты кончились |

## Маршрутизация задач

1. Полный код в брифе, 1–2 файла → **deepseek-chat**.
2. Требуется вывести/спроектировать (математика, новые ядра) → **deepseek-v4-pro**.
3. Многофайловая интеграция, git-операции, харнесс-специфика → **Haiku** (чёткий бриф) или **Sonnet 5** (нужно суждение).
4. Провал → эскалация на ступень; вторая ступень провалила → бриф виноват, чинит архитектор.
5. Всё проходит ревью архитектора: диф читается глазами, тесты перепроверяются,
   clippy `-D warnings`, критика письменно (что не так и почему), исполнитель переделывает.

## Инфраструктура

- `dev_agent.py` (scratchpad): DeepSeek-исполнитель с песочницей — `read_file`/`write_file`/`list_dir`
  строго внутри репо (без `.git`), `run_cargo` с белым списком (test/build/clippy/fmt).
- **cargo-раннер** (`.superpowers/runner/cargo_runner.sh`, запускает архитектор своим шеллом):
  Windows Application Control блокирует rustc в процессной цепочке python → cargo
  исполняется внешним bash-циклом через файловый протокол request.json/response.json.
  Перед сессией dev_agent раннер должен быть жив.
- Системные промпты исполнителей: рядом, в `docs/process/prompts/`.
- Ключи API: `~/.ggrs-secrets.json` — вне репозитория.

## Дисциплина промптов для исполнителей (выжимка уроков)

- «У тебя НЕТ инструментов кроме перечисленных» — без этого minimax/qwen галлюцинируют tool-вызовы.
- «Только факты из фактически прочитанного» — иначе реконструкции по абстрактам.
- «Перед правкой — прочитай файл; после правки — тесты; финал — отчёт без tool-вызовов».
- Тесты исполнителей проверять на то, что они действительно исполняют граф/код
  (урок Task 9 второго круга: тест без вызова compute сравнивал нули).
- Точные сигнатуры Interfaces в каждом брифе — исполнитель не видит соседних задач.
